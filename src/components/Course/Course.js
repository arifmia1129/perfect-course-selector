import React from 'react';
const Course = ({ course }) => {
    const { name, price, img } = course;
    console.log(name);
    return (
        <div>

        </div>
    );
};

export default Course;